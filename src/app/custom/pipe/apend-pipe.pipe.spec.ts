import { ApendPipePipe } from './apend-pipe.pipe';

describe('ApendPipePipe', () => {
  it('create an instance', () => {
    const pipe = new ApendPipePipe();
    expect(pipe).toBeTruthy();
  });
});
